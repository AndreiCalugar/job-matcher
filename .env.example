# Supabase project → Settings → API. Server-only; never NEXT_PUBLIC_.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Basic-auth gate for deployed environments (src/proxy.ts). Unset locally = open.
APP_BASIC_USER=
APP_BASIC_PASS=
